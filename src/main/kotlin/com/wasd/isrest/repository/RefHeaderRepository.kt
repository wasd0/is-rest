package com.wasd.isrest.repository

import com.wasd.isrest.domain.RefHeader
import org.springframework.data.jpa.repository.JpaRepository

interface RefHeaderRepository : JpaRepository<RefHeader, Int> 